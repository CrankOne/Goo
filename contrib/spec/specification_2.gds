{
    a = 5;
    f(x) = x%a;
    \lambda[[^\mu,\nu]]() <- {
            {1, 0},
            {0, 1},
        };

    boxes = Geo::Box( {
            {-1., -1., 1., 1.},
            {-a,  -a,   a,  a},
        } );
}

# Разница между `=' и `<-' в том что первое — causes немедленный eval(rvalue),
# второе не causes.
