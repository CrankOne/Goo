
# Arithmetic
2.34/2; # Should produce out[0] : 1.17

# Simple variable
a = 1;  # should produce out[1] : Int16 a = 1;

# Arithmetic result into variable:
b = 5*8;  # should produce out[2] : Int16 b = 40;

# Fractions:
c = 3/4;  # out[3] : Rational16 c = 3/4;

# Functions:
f( _x ) <- a * _x^2;  # out[4] : f(x) := x^2;

#
#
#

F(x) <- M::Integrate( f, [x, 0, 5] );

